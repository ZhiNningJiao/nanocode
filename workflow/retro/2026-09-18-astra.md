# 复盘摘要 — Astra 班次（2026-09-17 09:16 → 09-18 13:08，27.86h）

全文：`~/code/astra-retro-rootcause-1335/ROOT_CAUSE.md`（171 行，含根因表与占比）。
脚本化清单：本目录 `SCRIPT_BACKLOG.md`（Top3 可直接派单）。文档瘦身提案：`~/code/astra-retro-rootcause-1335/DOC_DIET.md`。

## 大白话（≤200 字）
最大的坑是「叫醒秘书的电话线断了 22 小时没人知道」：waker 283 次注入仅 4 次确认
（1.4%），打不通直接丢弃不重拨，一次车卡死红灯 1h52m 才被看见，5 小时零产出。
次坑：Astra 55% 动手时间花在手工 grep 翻代码（31% 的动手是裸 grep），现成 CodeKG
速查库没用上。脚本并不少（90 个 .sh + 38 条 cron），缺的是**看守卫的卫兵**——全仓
grep「UNCONFIRMED」零命中。文档脏（141KB 四份）属实但排第 4 位，不是主因。

## 立法产物（本工作流已吸收）
- 派单书武器库段焊进正门（CodeKG + ARSENAL_INDEX + memq）——治根因 3。
- ckf-first 壳：`bash ~/code/ckg-first-1355/bin/ckg-first.sh <关键词>` 先查 CodeKG。
- 时间戳纪律（真实 date 输出，禁手抄）——治三车连犯。
- S1 `waker-inject-health.sh`（P0）：注入确认率<50% 或 >30min 无确认 → 飞书降级告警 owner（预估回收 ~11h/班次）。
- S2 `shift-throughput-selfcheck.sh`（P0）：班次吞吐自检。
